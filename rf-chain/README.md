# RF Receive Chain

```
Antenna → ZX60-P162LN+ (LNA) → ZX75BP-1450-S+ (BPF) → ZX60-V63+ (Amp) → SDR
```

Center frequency: 1420.405 MHz | Bandwidth: ~50 MHz

Datasheets in `datasheets/`, cascade analysis in `docs/`.
