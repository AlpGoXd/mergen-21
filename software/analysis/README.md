# Observation Analysis Scripts

Python scripts for HI 21 cm data processing.

Workflow: raw SDR data → PSD → calibration → line profile → velocity → rotation curve.

Dependencies: `../requirements.txt`
